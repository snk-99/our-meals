{{!-- {{#each meals as |meal|}}
  <section class='card'>
    <a href='/meal/{{id}}'>
      <header>{{name}}</header>
      <img src='/images/{{meals.0.filename}}' />
    </a>
  </section>
{{/each}} --}}